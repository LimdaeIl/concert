server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: backend-app

  jackson:
    time-zone: Asia/Seoul

  config:
    import: optional:file:./.env[.properties]

  profiles:
    active: dev