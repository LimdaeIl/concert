<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.concert.backend.venuehall.query.AdminSeatQueryMapper">

  <sql id="searchConditions">

    WHERE s.venue_hall_id = #{condition.venueHallId}

    <if test="condition.keyword != null and condition.keyword != ''">
      AND (
      s.section_name LIKE CONCAT(
      '%',
      #{condition.keyword},
      '%'
      )
      OR s.row_name LIKE CONCAT(
      '%',
      #{condition.keyword},
      '%'
      )
      OR s.seat_number LIKE CONCAT(
      '%',
      #{condition.keyword},
      '%'
      )
      )
    </if>

    <if test="condition.floor != null">
      AND s.floor = #{condition.floor}
    </if>

    <if test="condition.seatType != null">
      AND s.seat_type = #{condition.seatType}
    </if>

    <if test="condition.status != null">
      AND s.status = #{condition.status}
    </if>

  </sql>


  <select
    id="findAll"
    resultType="com.concert.backend.venuehall.query.AdminSeatQueryRow"
  >

    SELECT
    s.id AS seatId,
    s.venue_hall_id AS venueHallId,
    s.section_name AS sectionName,
    s.floor AS floor,
    s.row_name AS rowName,
    s.seat_number AS seatNumber,
    s.seat_type AS seatType,
    s.status AS status

    FROM v1_seats s

    <include refid="searchConditions"/>

    ORDER BY
    s.floor ASC,
    s.section_name ASC,
    s.row_name ASC,
    s.seat_number ASC,
    s.id ASC

    LIMIT #{condition.size}
    OFFSET #{condition.offset}

  </select>


  <select
    id="count"
    resultType="long"
  >

    SELECT COUNT(*)

    FROM v1_seats s

    <include refid="searchConditions"/>

  </select>

</mapper>
